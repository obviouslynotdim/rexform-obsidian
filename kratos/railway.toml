[build]
builder = "DOCKERFILE"
dockerfilePath = "kratos/Dockerfile"

[deploy]
startCommand = "kratos serve --config /etc/config/kratos/kratos.yml --watch-courier"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
